# 复制本文件为 .env 并填入真实口令（.env 已被 .gitignore 忽略，不会提交）。
# docker compose 会自动读取同目录的 .env。
# 未填写的变量会让 docker compose 直接报错退出——这是故意的，避免默默用默认口令。

MYSQL_ROOT_PASSWORD=
REDIS_PASSWORD=

# 数据库用户名（可不填，默认 root）
DB_USERNAME=root
